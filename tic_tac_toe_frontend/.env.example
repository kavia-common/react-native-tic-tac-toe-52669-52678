# Expo public env vars for this container.
# Copy to .env and set values as needed for your environment.
# NOTE: Do NOT commit real secrets. This app currently does not require secrets.

EXPO_PUBLIC_PORT=
EXPO_PUBLIC_TRUST_PROXY=
EXPO_PUBLIC_LOG_LEVEL=
EXPO_PUBLIC_HEALTHCHECK_PATH=
EXPO_PUBLIC_FEATURE_FLAGS=
EXPO_PUBLIC_EXPERIMENTS_ENABLED=
